{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "skipLibCheck": false
    },
    "include": ["dist/**/*.d.ts"],
    "exclude": ["dist/amd/*"] //TODO: dont duplicate type defs, theyre exactly the same
}
